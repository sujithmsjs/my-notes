Projects:

1. Create two components App and Box. Box contains a number in it. App contains a button. Whenever App's button is clicked Box's number has to get updated with random number.

onDeleteRecord
onEditRecord